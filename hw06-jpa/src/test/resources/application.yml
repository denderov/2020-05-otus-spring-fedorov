spring:
  shell:
    interactive:
      enabled: false
  datasource:
    url: jdbc:h2:mem:testdb
    initialization-mode: always
  liquibase:
    enabled: true
    change-log: db/changelog/db.changelog-test.yaml
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true